<%= form_for(@cast, url: person_create_cast_path(@person)) do |f| %>
  <% if @cast.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cast.errors.count, "error") %> prohibited this cast from being saved:</h2>

      <ul>
      <% @cast.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :movie_id %>
    <%= f.select :movie_id,
    Movie.all.map { |m| [ m.title, m.id ] } %>
  </div>

  <div class="field">
    <%= f.label :role %>
    <%= f.select :role, 
       Cast::ROLES.map {|r| [r.titleize, r]} %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
